Zapytanie SELECT:

SELECT * FROM nazwa_tabeli;
Powyższe zapytanie wybiera wszystkie kolumny (asterisk *) z danej tabeli.

Zapytanie WHERE:

SELECT * FROM nazwa_tabeli WHERE warunek;
Powyższe zapytanie wybiera wszystkie rekordy z danej tabeli, które spełniają podany warunek.

Zapytanie INSERT:

INSERT INTO nazwa_tabeli (kolumna1, kolumna2, ...) VALUES (wartość1, wartość2, ...);
Powyższe zapytanie dodaje nowy rekord do danej tabeli, podając wartości dla odpowiednich kolumn.

Zapytanie UPDATE:

UPDATE nazwa_tabeli SET kolumna = nowa_wartość WHERE warunek;
Powyższe zapytanie aktualizuje wartość określonej kolumny w rekordach spełniających podany warunek.

Zapytanie DELETE:

DELETE FROM nazwa_tabeli WHERE warunek;
